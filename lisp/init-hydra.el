;;; init-hydra.el --- Hydra configuration -*- lexical-binding: t -*-
;;; Commentary:

;; I know hydra is very powerful, but actually I am not goot at using it.
;; I think hydra is a great and powerful package,
;; so I move it from `init-efficiency.el` to an indepent file.

;;; Log:

;;; Code:

;; hydra
(use-package hydra
  :ensure t)

(provide 'init-hydra)
;;; init-hydra.el ends here
